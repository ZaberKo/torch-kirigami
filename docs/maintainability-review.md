# 代码可读性与维护性审阅

审阅日期：2026-09-13。范围是 `torch_kirigami/` 全部生产 Python 模块，结合相关测试、公开使用流程，以及此前独立 review 记录中的优化建议。参考仓库和实验脚本不参与本次重构。

结论：现有职责边界可以保留，需要收拢重复知识、删除无用工作，并把长流程中相对独立的步骤命名清楚。本轮未增加类、协议、兼容层或新的执行框架。

## 本轮已经修改

- **建图流程分清职责。** [graph.py](../torch_kirigami/graph.py) 的主流程负责捕获和绑定；`_analyze_operation()` 负责一次调用的规则及诊断；`_finalize_analysis()` 负责全图约束、邻接关系和只读记录。处理顺序与原来一致，没有引入 Builder。
- **删除不使用的数据。** [bindings.py](../torch_kirigami/bindings.py) 的容器遍历不再构造所有调用方都丢弃的路径。循环、共享容器和字典键检查保留。[capture.py](../torch_kirigami/capture.py) 对同一次调用只查询一次 `CallEffects`，复用同一份写入和新存储事实。
- **尺寸语义集中维护。** reshape 的尺寸表达式解析移到已有的 [shapes.py](../torch_kirigami/operators/shapes.py)，与求值和约束放在一起。分析、配方和执行前校验重复的分区拼接形状计算，统一使用 [regions.py](../torch_kirigami/regions.py) 的 `concatenated_shape()`；各层仍决定如何报告错误。
- **索引算法表达清楚。** [relations.py](../torch_kirigami/relations.py) 的块映射改用来源、目标和块宽度的完整名称。完整块与触及块分别明确向内、向外取整；压缩区间算法、双向语义和限额保持。
- **验证流程减少嵌套和隐式依赖。** [validation.py](../torch_kirigami/pruning/validation.py) 单独组织 meta 模块构造；错误提示作为参数传递；不再反复建立同一 requirement 字典或扫描所有操作寻找生产者。形状、原坐标、布局和原地写入检查保留。
- **属性记录不再伪装成修改配方。** [state.py](../torch_kirigami/pruning/state.py) 的受管理属性记录直接接收路径，checkpoint 不再创建 `AttributeRecipe(path, None, None)`。共享属性用一份明确的赋值字典检查冲突，包含“一个别名要求不变，另一个要求修改”的情况。这个精简同时修复了真实漏检；冲突现在在提交前拒绝。
- **稀疏正则分开常规归约和回退。** [values.py](../torch_kirigami/sparsity/values.py) 用提前返回识别整轴区域，替代双层 `for/else/break`。一次计算共用一份已验证绑定；不规则区域和数值回退共用读取函数，避免重新扫描整张图。没有跨调用缓存权重或 autograd 图。

## 保留的复杂度及原因

- **区域并集、分组布局、共享参数的一致性、增量传播。** grouped/depthwise Conv、重叠请求和多消费者都依赖这些语义。把它们简化为单轴集合或一次遍历会重新删错位置。
- **捕获隔离、过期检查、事务提交与撤销。** 它们保护用户模型和调用方状态。检查关注的事实不同，不能因为都叫“校验”就合并或删除。
- **有限缓存与最终独立验证。** 现有缓存减少同一轮规划中的重复分析；用户回调和最终提交之间仍需要重新验证。保留已有边界，本轮不增加缓存层。
- **稳定范数与异常数值回退。** 这些分支有独立数值、梯度和极值回归依据。直接替换成平方求和会重新出现中间溢出或错误零梯度。本轮改善调用组织，不改数学算法。
- **不同用途的模块副本。** meta 检查、捕获隔离和 checkpoint 加载对张量、属性及副作用的要求不同。底层可复用的状态操作继续复用，完整流程分别组织。

## 先前优化建议的取舍

已采用并继续保留的简单优化：有序区间双指针运算、空选择提前返回、批量归并映射区间、参数组先分桶再精确比较、整轴正则批量归约、避免确定多余的 gather/复制。它们有局部适用条件，且不改变坐标和事务语义。

暂不新增以下优化：

- **统一所有副本用途的“通用复制引擎”。** 会引入多种策略开关，把不同生命周期的细节耦合起来。
- **更复杂的跨请求增量求解和缓存。** 收益还需针对真实模型测量，失效规则和用户回调会增加维护成本。
- **ChannelShuffle 的进一步关系压缩。** 当前语义已经正确；没有新的性能证据时，不增加专用压缩算法。
- **按原生 loader 类型细分 checkpoint 快速路径。** 暂不增加 BatchNorm 或其他模块专用加载分支，保留清楚的隔离边界。
- **缓存设备索引张量或扩展混合精度 gate 策略。** 需要额外的设备、失效和精度契约，作为独立需求评估。

上一轮删除的 CPU/cuDNN 输出布局推测不恢复。未知布局由现有契约传递，需要具体 stride 保证时再给出诊断。

## 验证原则

本轮新增测试覆盖：效果回调只查询一次；共享属性冲突的顺序无关性、计划加载、提交前拒绝及失败状态；混合规则/非规则/标量参数组的独立目标和梯度、重复组、两类数值回退、训练后新值与非有限值。

其余重构复用现有公共 build/plan/apply/save/load、分组卷积、形状来源、区域映射和事务回归。架构测试继续检查完整导入图无环、没有局部导入或 `TYPE_CHECKING` 隐藏依赖。测试覆盖的是这些契约，不能替代未来变更的审阅。


本轮整合后的验证结果：

- Python 3.10 / PyTorch 2.6 CPU：1831 passed，1442 CUDA 参数项跳过。
- Python 3.12 / PyTorch 2.14 CPU：1831 passed，1442 CUDA 参数项跳过。
- PyTorch 2.14 CUDA，`--require-cuda` 全量：3272 passed，1 个不适用的 CPU 跨设备分支跳过；CUDA 参数项实际执行。
- Ruff check、Ruff format、导入架构检查、四个基础示例及 sdist/wheel 构建通过。
